import 'package:flutter/material.dart';

class TabletResonsive extends StatelessWidget {
  const TabletResonsive({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Welcome To Tablet Version"),
      ),
    );
  }
}
